#include "main.h"
/**
 * add - adds two numbers
 * @a: one of the numbers to be added
 * @b: the other number to perform the addition
 *Return: the result of the addition
 */
int add(int a, int b)
{
	return (a + b);
}
